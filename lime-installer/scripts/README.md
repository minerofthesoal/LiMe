# LiMe Installer Scripts

This folder is reserved for post-install and pre-install helper scripts.

Suggested scripts:
- `hardware-detect.sh`
- `driver-autoinstall.sh`
- `timezone-sync.sh`

The build pipeline now creates this path by default and can safely include it
inside generated ISO root filesystems.
