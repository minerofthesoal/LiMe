# LiMe Packager Workspace

Packager helpers and metadata can live here.

Example contents:
- package manifests
- release notes templates
- package signing scripts

This scaffold unblocks build jobs that expect `build-tools/packager` to exist.
